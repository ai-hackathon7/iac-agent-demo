---

## ASSUMPTIONS

### 🔴 Required — Must Be Resolved Before Deploying

| # | Assumption | Impact |
|---|-----------|--------|
| 1 | **`env`** is not specified in the plan. Defaults to `dev` in `cdk.json`. Must be one of `dev`, `staging`, `prod` per org tagging policy. Pass via `-c env=<value>`. | Tagging policy violation if wrong value used |
| 2 | **`project`** short name is not specified. Defaults to `myapp` in `cdk.json`. All resource names and exports use this value. | All resource names will be incorrect |
| 3 | **`team`** is not specified. Defaults to `platform` in `cdk.json`. `Team` is a **required** tag per org policy. | Tagging policy violation |
| 4 | **`costCenter`** is not specified (`CC-XXXX` is a placeholder). Defaults to `CC-0000`. Must match pattern `^CC-[0-9]{4}$`. | Tagging policy violation |

### 🟡 Inferred — Verify Before Deploying

| # | Assumption | Reasoning |
|---|-----------|-----------|
| 5 | **`ManagedBy` tag set to `cdk`**, not `terraform` as in the plan. | The plan's tag value was likely a copy-paste artefact — this is CDK code, not Terraform. |
| 6 | **`singleNat: true`** (1 NAT gateway across all AZs). | Plan explicitly set `singleNat: true`. ⚠️ Change to `false` for production-grade HA. |
| 7 | **Subnet CIDR mask set to `/20`** (4096 IPs per subnet). | Derived from the org module's `cidrsubnet(/16, 4, n)` formula in the template. |
| 8 | **No org module exists for Security Groups** (`security-group` module returned 404). | Implemented as a raw CDK L2 `ec2.SecurityGroup` construct — acceptable per the plan's `module: null`. |
| 9 | **`allowAllOutbound: false`** on the SG, with an explicit egress rule for all IPv4. | Mirrors the plan's explicit egress rule definition; CDK's default `true` would silently manage egress. |
| 10 | **No bastion EC2 instance** is provisioned. | The plan only defines the SG. The instance itself can reference the exported `BastionSecurityGroupId` output when added later. |
| 11 | **`DataClassification` and `Backup` optional tags omitted.** | Not specified in the plan. Add to stack tags if required by project policy. |

### 🚀 Deployment Commands